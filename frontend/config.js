/**
 * Q-Aura Frontend — Configuração de Ambiente
 *
 * ✅ DESENVOLVIMENTO LOCAL (padrão):
 *   Não precisa alterar nada. O app detecta automaticamente
 *   que está na porta 3000 e aponta para localhost:8000.
 *
 * 🚀 PRODUÇÃO (EasyPanel/VPS):
 *   Este arquivo NÃO precisa ser alterado se o frontend estiver
 *   sendo servido pelo próprio backend (mesma origem).
 *
 *   Se o frontend estiver em domínio separado (ex: Netlify/Vercel),
 *   defina a variável abaixo com a URL do backend:
 *
 *   window.QAURA_API_OVERRIDE = "https://api.q-aura.com";
 *
 * ⚠️  NÃO commite tokens ou senhas neste arquivo.
 */

// Descomente e edite a linha abaixo apenas se usar frontend separado do backend:
// window.QAURA_API_OVERRIDE = "https://api.q-aura.com";
